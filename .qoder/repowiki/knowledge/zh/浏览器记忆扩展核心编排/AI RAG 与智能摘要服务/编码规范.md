- 所有 AI 客户端均注入自定义 `fetcher` 函数以支持测试隔离或环境适配，默认绑定到 `self.fetch`。
- 网络请求统一采用 30 秒超时机制，并通过 `AbortController` 实现中断处理。
- API 错误处理标准化，将 HTTP 状态码映射为统一的 `OpenAIRequestError` 及其错误码（如 authentication, rate_limit）。
- 提示词管理采用 locale-keyed 映射结构，通过 `getPrompts(locale)` 获取对应语言的模板，未匹配时回退至默认中文。