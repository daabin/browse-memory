- `capture` 子包包含无状态策略函数 `shouldCapture` 和基于事件驱动的会话状态机 `transitionSession`，负责追踪标签页活跃时长。
- `extraction` 子包封装 `@mozilla/readability` 库，提供 `extractPage` 函数从 DOM 中提取标题和正文，具备异常降级处理。
- 模块通过 `src/shared/` 下的类型和 URL 策略共享基础定义，保持捕获逻辑与具体浏览器 API 解耦。