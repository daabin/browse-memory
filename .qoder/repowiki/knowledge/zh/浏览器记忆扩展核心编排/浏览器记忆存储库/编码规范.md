- 所有仓储类均采用构造函数依赖注入模式，接收 `BrowseMemoryDatabase` 实例作为唯一私有只读属性。
- 涉及多表写操作或保证数据一致性的方法（如删除会话及其消息）均使用 `this.database.transaction` 包裹。
- 数据过期清理逻辑统一命名为 `purgeExpired`，并接受 `retentionDays` 和可选的 `now` 时间戳参数。
- 主键生成统一使用 Web Crypto API 的 `crypto.randomUUID()`，而非数据库自增 ID。