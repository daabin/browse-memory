- 采用点号分隔的键名路径（如 `sidepanel.memory`）在 JSON 语言包中组织翻译文案，并通过递归查找实现嵌套取值。
- UI 组件统一使用 `clsx` 结合 `tailwind-merge` 的 `cn` 工具函数处理动态类名合并，确保 Tailwind 样式优先级正确。
- 共享类型定义集中在 `src/shared/types.ts`，并在消息协议 `messages.ts` 中通过 `import type` 引入，实现逻辑层与视图层的类型解耦。