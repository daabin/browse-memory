- 所有 React 入口点 (`main.tsx`) 均使用 `React.StrictMode` 和 `I18nProvider` 包裹根组件，确保开发模式检查与国际化上下文可用。
- UI 入口点组件 (`App.tsx`) 支持通过 props 注入客户端实例 (如 `dashboardClient`, `runtimeClient`)，便于单元测试与模拟环境运行。
- 后台脚本 (`background.ts`) 采用函数式导出 `defineBackground`，并在内部完成所有服务类的实例化与事件绑定，遵循单一入口初始化模式。