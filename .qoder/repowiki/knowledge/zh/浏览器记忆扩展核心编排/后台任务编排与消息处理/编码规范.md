- 所有涉及用户隐私的 API Key 在存入数据库前必须通过 `SecretStore` 进行 AES-GCM 加密，读取时动态解密。
- 耗时或可重试的后台操作（如向量化、摘要生成）不直接执行，而是通过 `TaskQueue` 入队并由 `TaskRunner` 异步批处理。
- 消息处理器采用统一的 try-catch 包装，区分 `OpenAIRequestError` 与未知异常，确保返回结构一致的 `RuntimeResponse`。