- 核心检索层：`hybrid-search.ts` 作为入口，通过倒数排名融合（RRF）算法合并 `bm25.ts` 的关键词评分与 `vectorSearch` 的余弦相似度评分。
- 索引管理层：`bm25.ts` 实现了内存中的倒排索引结构，支持文档的动态增删（`upsertDocument`/`removeDocument`）及持久化加载（`loadStoredIndex`）。
- 基础工具层：`tokenize.ts` 利用 `Intl.Segmenter` 实现中英双语分词及停用词过滤；`snippet.ts` 负责根据查询词在原文中定位并截取带高亮坐标的摘要片段。